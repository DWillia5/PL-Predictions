#TODO - rake task to get fixtures and place in model.

# include MatchGrabber::GetMatch

# a = get_fixtures
# a.each_pair do |date, matches|
#     b = Fixture.new
#   ^C
# a.each_pair do |date, matches|
#     matches.each do
#       ^C
# a.each_pair do |date, matches|
#     matches.each do |match|
#       b = Fixture.new
#     b.match_date = date
#     b.team_a_name = match.first
#     b.team_b_name = match.last
#     b.save
#     end
#   end